{
  "name": "offer-guard-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Privacy-first API for deterministic job-offer risk analysis.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "mongoose": "^8.16.4"
  }
}
